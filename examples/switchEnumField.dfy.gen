// Generated by lsc from switchEnumField.ts
// LemmaScript string model: javascript-utf16-code-units

datatype Tag = read | write | exec

datatype Perm = Perm(tag: Tag, level: int)

function weight(p: Perm): int
{
  match p.tag {
    case read =>
      1
    case write =>
      2
    case exec =>
      3
  }
}

lemma weight_ensures(p: Perm)
  ensures (weight(p) >= 1)
{
}

method pickPlainString(kind: string) returns (res: int)
  ensures (res >= 0)
{
  match kind {
    case "small" =>
      return 1;
    case "medium" =>
      return 2;
    case "large" =>
      return 3;
    case "15m" =>
      return 4;
    case _ =>
      return 0;
  }
}
