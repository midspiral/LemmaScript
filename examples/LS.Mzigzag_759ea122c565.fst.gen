// Generated by lsc. Program source: zigzag.ts
// Add proofs in the .fst; regenerate with lsc regen --backend=fstar.
module LS.Mzigzag_759ea122c565

module R = LS.Runtime
module S = FStar.Sequence
module FS = FStar.FiniteSet.Base
module FM = FStar.FiniteMap.Base
open FStar.FiniteSet.Ambient
open FStar.FiniteMap.Ambient
open FStar.Real

let v_encode  (v_n:int)
  : Ghost int
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        (ls_result >= (0))
      )) =
  (if (v_n >= (0)) then ((2) * v_n) else (((-2) * v_n) - (1)))

let v_decode  (v_z:int)
  : Ghost int
      (requires (
        (v_z >= (0))
      ))
      (ensures (fun ls_result ->
        True
      )) =
  (if (R.decide (((R.rem (v_z) ((2))) == (0)))) then (R.floor_div (v_z) ((2))) else (- (R.floor_div ((v_z + (1))) ((2)))))

let v_roundTrip  (v_n:int)
  : Ghost int
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        (ls_result == v_n)
      )) =
  (v_decode ((v_encode (v_n))))

let v_reverseRoundTrip  (v_z:int)
  : Ghost int
      (requires (
        (v_z >= (0))
      ))
      (ensures (fun ls_result ->
        (ls_result == v_z)
      )) =
  (v_encode ((v_decode (v_z))))
