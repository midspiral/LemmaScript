// Generated by lsc. Program source: perm.ts
// Add proofs in the .fst; regenerate with lsc regen --backend=fstar.
module LS.Mperm_eecd295aff45

module R = LS.Runtime
module S = FStar.Sequence
module FS = FStar.FiniteSet.Base
module FM = FStar.FiniteMap.Base
open FStar.FiniteSet.Ambient
open FStar.FiniteMap.Ambient
open FStar.Real

let rec v_countOn  (v_xs:(S.seq bool))
  : Ghost int
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        ((0) <= ls_result) /\ (ls_result <= (S.length (v_xs)))
      ))
      (decreases (S.length (v_xs))) =
  (if (R.decide (((S.length (v_xs)) == (0)))) then (0) else ((if (S.index (v_xs) ((0))) then (1) else (0)) + (v_countOn ((R.slice (v_xs) ((1)) ((S.length (v_xs))))))))

let v_permRefl  (v_xs:(S.seq bool))
  : Ghost bool
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        (R.perm (v_xs) (v_xs))
      )) =
  true

let v_permSymm  (v_xs:(S.seq bool)) (v_ys:(S.seq bool))
  : Ghost bool
      (requires (
        (R.perm (v_xs) (v_ys))
      ))
      (ensures (fun ls_result ->
        (R.perm (v_ys) (v_xs))
      )) =
  true

let v_permConcatComm  (v_xs:(S.seq bool)) (v_ys:(S.seq bool))
  : Ghost bool
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        (R.perm ((S.append (v_xs) (v_ys))) ((S.append (v_ys) (v_xs))))
      )) =
  true

let rec count_on_occurs (xs:S.seq bool)
  : Lemma (v_countOn xs == R.occurs xs true) (decreases (S.length xs)) =
  if S.length xs > 0 then (
    assert (S.equal (R.slice xs 1 (S.length xs)) (S.drop xs 1));
    count_on_occurs (S.drop xs 1)
  )

let v_countOnPerm  (v_xs:(S.seq bool)) (v_ys:(S.seq bool))
  : Ghost bool
      (requires (
        (R.perm (v_xs) (v_ys))
      ))
      (ensures (fun ls_result ->
        ((v_countOn (v_xs)) == (v_countOn (v_ys)))
      )) =
  count_on_occurs v_xs;
  count_on_occurs v_ys;
  true
