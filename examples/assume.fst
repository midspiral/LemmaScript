// Generated by lsc. Program source: assume.ts
// Add proofs in the .fst; regenerate with lsc regen --backend=fstar.
module LS.Massume_0a9716434fb1

module R = LS.Runtime
module S = FStar.Sequence
module FS = FStar.FiniteSet.Base
module FM = FStar.FiniteMap.Base
open FStar.FiniteSet.Ambient
open FStar.FiniteMap.Ambient
open FStar.Real

// Trusted source annotation: havoc. Each invocation has its own trace.
assume val ls_havoc_4 : list int -> GTot R.string

let v_shortenString  (ls_trace:list int) (v_text:R.string)
  : Ghost int
      (requires (
        True
      ))
      (ensures (fun ls_result ->
        (ls_result >= (0)) /\ (ls_result <= (S.length (v_text)))
      )) =
  let v_cleaned_3 : R.string = (ls_havoc_4 (ls_trace)) in
  assume (((S.length (v_cleaned_3)) <= (S.length (v_text))));
  (S.length (v_cleaned_3))
