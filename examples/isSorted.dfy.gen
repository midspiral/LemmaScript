// Generated by lsc from isSorted.ts

function isSorted_loop(arr: seq<int>, result: bool, i: nat): bool
  requires (|arr| > 0)
  decreases (|arr| - i)
{
  if ((i + 1) < |arr|) then
    if (arr[i] > arr[(i + 1)]) then
      var result := false;
      i
    else
      var i := (i + 1);
      isSorted_loop(arr, result, i)
  else
    result
}

function isSorted(arr: seq<int>): bool
  requires (|arr| > 0)
{
  isSorted_loop(arr, true, 0)
}
