/-
  Generated by lsc — Lean types and pure function mirrors.
-/
import LemmaScript

namespace Pure

def dedupSize (arr : Array Int) : Int :=
  let s := Std.HashSet.ofList arr.toList
  s.size

def member (arr : Array Int) (x : Int) : Bool :=
  let s := Std.HashSet.ofList arr.toList
  s.contains x

end Pure
