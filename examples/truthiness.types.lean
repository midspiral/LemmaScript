/-
  Generated by lsc — Lean types and pure function mirrors.
-/
import LemmaScript

namespace Pure

def boolCond (b : Bool) : Int :=
  if b then
    1
  else
    0

def numCond (n : Int) : Int :=
  if n ≠ 0 then
    1
  else
    0

def numNot (n : Int) : Int :=
  if n = 0 then
    1
  else
    0

def numTernary (n : Int) : Int :=
  if n ≠ 0 then
    1
  else
    0

def strCond (s : String) : Int :=
  if s.length > 0 then
    1
  else
    0

def strNot (s : String) : Int :=
  if s = "" then
    1
  else
    0

def arrCond (xs : Array Int) : Int :=
  if true then
    1
  else
    0

def arrNot (xs : Array Int) : Int :=
  if false then
    1
  else
    0

def optNumCond (o : Option Int) : Int :=
  match o with
  | .some _o_val =>
    if _o_val ≠ 0 then
      1
    else
      0
  | .none =>
    0

def optNumNot (o : Option Int) : Int :=
  match o with
  | .some _o_val =>
    if _o_val ≠ 0 then
      0
    else
      1
  | .none =>
    1

def optStrCond (o : Option String) : Int :=
  match o with
  | .some _o_val =>
    if _o_val.length > 0 then
      1
    else
      0
  | .none =>
    0

def optPresent (o : Option Int) : Int :=
  match o with
  | .some _o_val =>
    1
  | .none =>
    0

end Pure
